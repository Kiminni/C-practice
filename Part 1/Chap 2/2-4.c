// 2-4.c
/*#include <stdio.h>
int main()
{
	printf("%d 더하기 %d 는 %d 입니다 \n",3,5,3+5); // 한글은 장식, 1번, 2번, 3번 순으로 따다닥 치환.(10진수 %d)
	printf("%i 더하기 %i 는 %i 입니다 \n",3,5,3+5); // 한글은 장식, 1,2,3 따다닥 치환(10진수 %i)

	printf("%d - %d = %d 입니다 \n",3,5,3-5); //(연산자는 다 데코, 1,2,3번 따다닥 치환)
	printf("%i - %i = %i 입니다 \n",3,5,3-5);	
	
	return 0;
} */
